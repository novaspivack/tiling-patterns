# Experiments

Exploratory **tiling-pattern CA** runs and visualizations.

```bash
conda activate tiling-patterns
python experiments/<script>.py
```

Outputs (PNGs, GIFs) go in `experiments/output/` (gitignored).

Promote stable rule logic to `src/tiling_patterns/` and lock behavior with `tests/`.
